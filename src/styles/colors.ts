export const colors = {
  primary: {
    from: "#fafafa", // Very light gray (light mode)
    via: "#f5f5f5", // Light gray (light mode)
    to: "#f0f0f0", // Slightly darker gray (light mode)
  },
  secondary: {
    from: "#121212", // Soft dark (dark mode)
    via: "#161616", // Slightly lighter dark (dark mode)
    to: "#1a1a1a", // Even lighter dark (dark mode)
  },
  accent: {
    from: "#3b82f6", // Blue accent (subtle)
    via: "#2563eb", // Darker blue
    to: "#1d4ed8", // Even darker blue
  },
};
